

Reactive Web Page Deployment using Jenkins + Docker

I’ve fixed formatting, script issues, and simplified everything so you can run without errors tomorrow.

✅ 🔹 OVERVIEW (What you are doing)
You will:

Run **Jenkins in Docker
Create a pipeline
Generate a reactive (JavaScript-based) website
Publish and view it inside Jenkins
Test dynamic behavior (button click updates content)
🔹 Step 0: Prerequisites
Install:

Docker Desktop
Docker Compose
👉 Start Docker before starting

🔹 Step 1: Create Project Folder
mkdir jenkins-reactive-site
cd jenkins-reactive-site
🔹 Step 2: Create docker-compose.yml (FIXED)
version: '3.8'

services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  jenkins_home:
🔹 Step 3: Start Jenkins
docker compose up -d
🔹 Step 4: Get Password
docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
🔹 Step 5: Open Jenkins
👉 Open browser:

http://localhost:8080
✔ Paste password
✔ Install suggested plugins
✔ Create admin user

🔹 Step 6: Install Plugin
👉 Go to:
Manage Jenkins → Plugins → Available Plugins

✔ Install: HTML Publisher Plugin

🔹 Step 7: Create Pipeline Job
👉 Dashboard → New Item

Name: karunya-reactive-site
Select: Pipeline
Click OK
🔹 Step 8: Add Pipeline Script (CORRECTED & WORKING)
👉 Pipeline → Definition: Pipeline script

Paste this:

pipeline {
    agent any

    stages {
        stage('Generate Reactive Site') {
            steps {
                script {
                    sh 'rm -rf site || true'
                    sh 'mkdir -p site/assets'

                    writeFile file: 'site/index.html', text: """
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<title>Karunya University — Reactive Site</title>
<link rel="stylesheet" href="assets/style.css"/>
</head>
<body>
<header class="site-header">
<div class="container">
<h1>Karunya University</h1>
<p class="tagline">Values — Knowledge — Service</p>
</div>
</header>

<main class="container">
<section class="card">
<h2 id="about-title"></h2>
<p id="about-text"></p>
<button onclick="updateContent()">Update Content</button>
</section>
</main>

<footer class="site-footer container">
<p>&copy; ${new Date().format('yyyy')} Karunya University — Generated by Jenkins</p>
</footer>

<script src="assets/app.js"></script>
</body>
</html>
"""

                    writeFile file: 'site/assets/style.css', text: """
body { font-family: Arial, sans-serif; margin:0; }
.container { max-width:800px; margin:0 auto; padding:20px; }
.site-header { background:#0b3d91; color:#fff; padding:20px; }
.card { background:#fff; border:1px solid #ddd; padding:15px; border-radius:6px; }
.site-footer { text-align:center; font-size:12px; color:#555; margin-top:20px; }
button { padding:8px 12px; cursor:pointer; }
"""

                    writeFile file: 'site/assets/app.js', text: """
let state = {
    title: "About Karunya University",
    description: "Karunya Institute of Technology and Sciences is a leading centre for higher education in India."
};

function render() {
    document.getElementById("about-title").innerText = state.title;
    document.getElementById("about-text").innerText = state.description;
}

function updateContent() {
    state.description = "This content was updated dynamically without page reload!";
    render();
}

document.addEventListener("DOMContentLoaded", render);
"""
                }
            }
        }

        stage('Publish Site') {
            steps {
                publishHTML([
                    allowMissing: false,
                    alwaysLinkToLastBuild: true,
                    keepAll: true,
                    reportDir: 'site',
                    reportFiles: 'index.html',
                    reportName: 'Karunya University - Reactive Site'
                ])
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: 'site/**', fingerprint: true
        }
    }
}
👉 Click Save

🔹 Step 9: Run Pipeline
👉 Click Build Now

✔ Wait until SUCCESS

🔹 Step 10: View Output
👉 Open build → Click:

Karunya University - Reactive Site
🔹 Step 11: Test Reactivity (IMPORTANT)
👉 Click button:

Update Content
✅ You will see:
👉 Text changes without page reload

✔ This proves it is a reactive webpage

🔹 Step 12: Download Output (Optional)
👉 Build page → Artifacts → Download site/

Open:

index.html
🔥 Common Errors (VERY IMPORTANT)
❌ Jenkins not opening
✔ Run:

docker compose up -d
❌ Port already in use
✔ Change:

- "9090:8080"
❌ Pipeline fails
✔ Use exact script (indentation matters)

❌ Button not working
✔ Ensure:

<script src="assets/app.js"></script>
❌ Page blank
✔ Check IDs:

about-title
about-text
🧠 Viva Short Answers
👉 Static vs Reactive webpage?

Static → fixed content
Reactive → updates dynamically using JavaScript
👉 What is reactivity?
Updating UI without reloading page

👉 What is Jenkins?
Automation tool for CI/CD

👉 What is pipeline?
Automated steps like build and deploy

👉 What is artifact?
Generated output files

✅ Final Memory Trick
👉 Docker → Jenkins → Pipeline → Generate JS site → Publish → Click button → Dynamic output

